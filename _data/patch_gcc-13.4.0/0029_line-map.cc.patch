diff -ruN gcc-releases-gcc-13.4.0/libcpp/line-map.cc ../_temp/gcc.work/libcpp/line-map.cc
--- gcc-releases-gcc-13.4.0/libcpp/line-map.cc	2025-06-05 18:03:58.000000000 +0200
+++ ../_temp/gcc.work/libcpp/line-map.cc	2026-03-17 22:34:22.915558190 +0100
@@ -1076,7 +1076,12 @@
 
   const line_map_ordinary *cached = LINEMAPS_ORDINARY_MAP_AT (set, mn);
   /* We should get a segfault if no line_maps have been added yet.  */
-  if (line >= MAP_START_LOCATION (cached))
+
+  #ifdef __amigaos4__
+  linemap_assert(cached != 0);
+  #endif
+
+   if (line >= MAP_START_LOCATION (cached))
     {
       if (mn + 1 == mx || line < MAP_START_LOCATION (&cached[1]))
 	return cached;
